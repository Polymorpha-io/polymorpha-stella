/**
 * Embedding chunking constants (single source, `D20`).
 * Moved verbatim from stella/models/embeddingModel.
 */

export const CHARS_PER_TOKEN = 4;
export const TOKEN_OVERLAP = 50;
export const CHAR_OVERLAP_FALLBACK = 200;
