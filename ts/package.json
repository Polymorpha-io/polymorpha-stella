{
  "name": "@polymorpha/stella-ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build 2>&1 | head -20; echo \"[stella] build done\"",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@polymorpha/business-logic": "git+https://github.com/Polymorpha-io/polymorpha-business-logic.git#main",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.1.6",
    "jsdom": "^29.1.1"
  }
}
